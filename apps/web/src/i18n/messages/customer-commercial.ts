import { defineMessages } from "../define";

/**
 * Commercial record messages (quotes, orders, agreements, buying, payment, offboarding). Owned by the customer lane's commercial half; new IDs use the `customer.commercial.` prefix.
 */
export const customerCommercialMessages = defineMessages({});
