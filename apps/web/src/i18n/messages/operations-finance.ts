import { defineMessages } from "../define";

/**
 * Finance operations messages (lifecycle, billing reconciliation, revenue, collections and corrections). Owned by the operations lane's finance half; new IDs use the `operations.finance.` prefix.
 */
export const operationsFinanceMessages = defineMessages({});
