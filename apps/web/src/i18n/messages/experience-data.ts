import { defineMessages } from "../define";

/**
 * Demo portal record and dashboard fixture messages. Owned by the experience lane's data half; new IDs use the `experience.data.` prefix.
 */
export const experienceDataMessages = defineMessages({});
