export * from "./platform";

import * as platformSchema from "./platform";

/** Lifecycle-platform lane additions belong here; foundation tables remain in ../../schema.ts. */
export const lifecycleSchema = platformSchema;
